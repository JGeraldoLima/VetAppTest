package com.example.petmedtracker.presentation.theme

import androidx.compose.ui.graphics.Color

// Palette from reference image — friendly, natural pet app
val BackgroundLight = Color(0xFFF5F5F5)       // Light gray / off-white — surfaces, cards
val SkyBlue = Color(0xFF8CD2FF)               // Light sky blue — primary (buttons, FAB, key actions)
val Tan = Color(0xFFC79F67)                   // Muted brown / tan — secondary, card accents
val Cream = Color(0xFFF2EAB8)                 // Pale yellow / cream — highlights, notes
val SoftGreen = Color(0xFFA4DB8A)             // Soft green — success, positive, confirmations

// Semantic mapping for Material 3
val Primary = SkyBlue
val OnPrimary = Color(0xFF1C1B1F)
val PrimaryContainer = Color(0xFFE3F2FD)
val OnPrimaryContainer = Color(0xFF001D35)

val Secondary = Tan
val OnSecondary = Color(0xFFFFFFFF)
val SecondaryContainer = Cream
val OnSecondaryContainer = Color(0xFF3D2E00)

val Tertiary = SoftGreen
val OnTertiary = Color(0xFF1C1B1F)
val TertiaryContainer = Color(0xFFE8F5E9)
val OnTertiaryContainer = Color(0xFF00210B)

val Surface = BackgroundLight
val OnSurface = Color(0xFF1C1B1F)
val SurfaceVariant = Color(0xFFE7E0EC)
val OnSurfaceVariant = Color(0xFF49454F)

val Error = Color(0xFFB3261E)
val OnError = Color(0xFFFFFFFF)

val Outline = Color(0xFF79747E)
